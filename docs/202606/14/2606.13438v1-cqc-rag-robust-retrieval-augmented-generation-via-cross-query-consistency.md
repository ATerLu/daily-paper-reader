---
title: "CQC-RAG: Robust Retrieval-Augmented Generation via Cross-Query Consistency"
title_zh: "CQC-RAG: 通过跨查询一致性实现鲁棒的检索增强生成"
authors: "Yanjia Sun, Sifan Liu, Jie Shao"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13438v1"
tags: ["query:rag-llm"]
score: 9.0
evidence: 提出一种通过跨查询一致性提高检索增强生成鲁棒性的新方法。
tldr: RAG虽提升大模型事实性，但检索结果易受查询形式影响，无关文档引发幻觉。现有方法通过多候选答案投票提升鲁棒性，但多样性注入不可控且答案评估限于单查询视角。本文提出跨查询一致性假设：正确答案在等价多样查询中置信度稳定，噪声幻觉则波动。据此设计CQC-RAG，重写问题为多样查询，重排序共享文档池，提取答案-证据对，依据跨上下文置信度稳定性选择答案。在TriviaQA和MuSiQue等基准上分别提升4.76和9.12个EM点，验证方法有效性。无需外部监督，为鲁棒RAG提供新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13438v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1416, \"height\": 912, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13438v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 425, \"height\": 785, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13438v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 886, \"height\": 985, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13438v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1835, \"height\": 499, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13438v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1832, \"height\": 474, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13438v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 880, \"height\": 416, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13438v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 881, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13438v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 876, \"height\": 162, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13438v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1488, \"height\": 382, \"label\": \"Table\"}]"
motivation: 现有RAG多路径方法多样性不可控且答案评估单视角，易受噪声幻觉影响。
method: CQC-RAG重写问题为多样查询，重排序文档池，提取答案-证据对，利用跨查询置信度稳定性筛选答案。
result: 在TriviaQA和MuSiQue上EM分别提升4.76和9.12个百分点，四个基准上均优于最强多查询基线。
conclusion: 验证了跨查询一致性假设，该方法无需外部监督即可有效过滤噪声幻觉，提升RAG鲁棒性。
---

## 摘要
检索增强生成（RAG）已成为提高大型语言模型事实准确性的常用方法，但其可靠性仍高度依赖于外部证据的检索和使用方式。语义等价但句法形式不同的查询可能导致不同的检索结果，而不相关或误导性的文档会进一步引发幻觉答案。现有多路径推理方法通过采样多个候选答案并应用基于投票或置信度的选择来提高鲁棒性，但仍面临两个局限：多样性通常通过不可控的解码随机性注入，而答案评估往往局限于单一查询引发的证据视角。为解决这些局限，我们提出跨查询一致性假设：正确答案在面对语义等价但句法多样的查询时倾向于保持高置信度，而噪声引起的幻觉在查询变化下则表现出不稳定的置信度。基于该假设，我们引入CQC-RAG框架，联合设计查询级多样性注入与跨查询一致性评估。CQC-RAG将原始问题重写为多样但语义不变的查询，对共享文档池重新排序以构建查询条件化的推理上下文，应用基于证据的协议提取答案-证据对，并根据答案在这些上下文中的置信度稳定性进行选择。该设计无需外部监督即可实现自我评估，且不依赖于扩展的检索覆盖。在四个开放域问答基准上的实验显示，CQC-RAG在TriviaQA上比先前最强的多查询基线高出+4.76个百分点（EM），在MuSiQue上高出+9.12个百分点（EM），验证了跨查询一致性在滤除噪声引发的幻觉方面的有效性。

## Abstract
Retrieval-Augmented Generation (RAG) has become a common approach for improving the factuality of Large Language Models (LLMs), yet its reliability remains highly sensitive to how external evidence is retrieved and used. Semantically equivalent queries with different syntactic forms may lead to different retrieval results, while irrelevant or misleading documents can further induce hallucinated answers. Existing multi-path reasoning methods improve robustness by sampling multiple candidate answers and applying voting- or confidence-based selection, but they still face two limitations: diversity is often injected through uncontrollable decoding randomness, and answer evaluation is usually confined to a single query-induced evidence view. To address these limitations, we propose a Cross-Query Consistency Hypothesis: correct answers tend to maintain high confidence across semantically equivalent but syntactically diverse queries, whereas noise-induced hallucinations exhibit unstable confidence under such query variations. Based on this hypothesis, we introduce CQC-RAG, a framework that co-designs query-level diversity injection with cross-query consistency evaluation. CQC-RAG rewrites the original question into diverse but meaning-preserving queries, reranks a shared document pool to construct query-conditioned reasoning contexts, applies an evidence-grounded protocol to extract answer-evidence pairs and selects answers according to their confidence stability across these contexts. This design enables self-evaluation without external supervision and does not rely on expanded retrieval coverage. Experiments on four open-domain question answering benchmarks show that CQC-RAG outperforms the strongest previous multi-query baseline by +4.76 pp EM on TriviaQA and +9.12 pp EM on MuSiQue, validating the effectiveness of cross-query consistency for filtering noise-induced hallucinations.

---

## 论文详细总结（自动生成）

### 1. 论文核心问题与整体含义（研究动机和背景）

*   **核心问题**：检索增强生成（RAG）系统虽然能提升大语言模型的事实准确性，但其可靠性极其脆弱。这主要体现在两个方面：
    *   **查询表述敏感性**：语义相同但句法不同的查询会导致检索结果显著差异（性能下降15-20%，极端情况超40%），进而影响最终答案。
    *   **噪声文档干扰**：检索到的无关或误导性文档会严重干扰模型推理，引发幻觉。
*   **现有方法局限**：现有多路径推理方法（如Self-Consistency）通过生成多个候选答案并投票来提高鲁棒性，但面临两大根本性挑战：
    *   **多样性注入不可控**：依赖解码层的温度采样，高温导致语义漂移，低温导致路径冗余，且无法改变单一查询固有的检索偏差。
    *   **答案评估辨别力有限**：多数投票或置信度评估仅在单一查询引发的单一证据视角下进行，无法识别被有偏或噪声证据持续强化的错误答案。
*   **整体含义**：本文旨在从根本上解决上述问题，提出了一种**跨查询一致性假设**：正确答案因扎根于真正相关的文档，在多个语义等价但句法多样的查询下应保持高置信度；而噪声引发的幻觉则因依赖虚假的、上下文相关的模式，其置信度会在查询变化时大幅波动。基于此，论文设计了一种无需外部监督、不依赖扩展检索覆盖的鲁棒RAG框架。

### 2. 论文提出的方法论：CQC-RAG 框架

CQC-RAG的核心思想是**联合设计查询级多样性注入与跨查询一致性评估**，具体分为三个阶段：

*   **阶段一：并行查询重写**
    *   **目标**：为原始查询 \( q_{org} \) 生成一组语义严格等价但句法高度多样的改写查询 \( Q \)。
    *   **关键技术细节**：
        *   **硬性约束**：强制冻结所有命名实体，防止检索漂移。
        *   **软性约束**：通过三个维度最大化句法多样性——词汇扰动（同义词替换非实体谓词）、句法重构（主被动语态转换、从句重排）和语用模态转换（疑问句与祈使句混用）。
    *   **与先前工作的区别**：多样性注入在查询层面而非解码层，旨在构建差异化的推理上下文，而非扩大检索覆盖范围。

*   **阶段二：跨查询重排序与推理**
    *   **查询特定重排序**：所有改写查询共享一个固定的初始文档池 \( D \)，但使用重排序器 \( M_{rerank} \) 根据每个查询 \( q_i \) 的语义相关性对 \( D \) 中的文档进行重新排序，生成查询特定的推理上下文 \( C_i \)。此举在不增加检索预算的前提下，创造出差异化的文档优先级排序。
    *   **基于证据的推理**：采用严格的“证据-摘录”协议。模型 \( M_{reason} \) 被要求首先从 \( C_i \) 中定位并提取支持答案的具体证据片段 \( E_i \)，然后**仅基于 \( E_i \)** 推导出最终答案 \( a_i \)，以强制答案忠实于源文本。所有查询的推理过程并行执行。

*   **阶段三：跨查询一致性置信度估计**
    *   **目标**：从候选答案集 \( A \) 中选出跨查询一致性最高的答案作为最终答案。
    *   **关键技术细节**：
        *   **跨查询一致性分数 \( S(a) \)**：由两部分构成，公式为 \( S(a) = \mu - \lambda(\mu) \cdot \text{Var} \)，其中 \( \mu = \mathbb{E}_{q \sim Q}[P(\text{Valid}|a, E, q)] \)。
            *   **语义共识**：对所有查询视角下验证为“有效”的概率求平均。
            *   **跨查询不稳定性**：计算验证概率的方差，并引入一个**自适应惩罚系数 \( \lambda(\mu) = \lambda_0 \cdot \mu \)**。这确保了对均值高但方差也高的候选答案施以更严惩罚，防止其因少数极端高分而胜出。
        *   **验证概率计算**：使用一个独立的评估模型 \( M_{eval} \)，通过单次前向传播，获取“Yes”和“No”标签的logits，并在二元空间内归一化得到 \( P(\text{Valid}) \)。该方法避免了自由生成带来的随机性和默许偏差，实现了确定性评估。
        *   **模型解耦**：评估模型与推理模型分离，以缓解“自我增强偏差”（即模型偏向于评分自己生成的答案）。

### 3. 实验设计

*   **数据集与场景**：在四个开放域问答基准上评估，覆盖单跳和多跳推理场景。
    *   **单跳数据集**：**TriviaQA**（事实知识检索，2000查询）、**PopQA**（长尾知识，1399查询）。
    *   **多跳数据集**：**HotpotQA**和**MuSiQue**（各采样1000查询，包含支持事实和混淆项的完整语料，严格测试多跳推理能力）。
*   **评估指标**：采用Exact Match (EM)、F1-score，并以两者的均值作为综合指标（AVG）。
*   **对比方法**：
    *   **标准RAG**：Mistral-7B-Instruct、Qwen3-8B、Llama-3.1-8B-Instruct（作为基准模型）。
    *   **现有多路径RAG**：Self-Certainty、Speculative RAG、DMQR-RAG、SPARC-RAG。所有对比方法均使用相同的骨干模型（Qwen3-8B）和统一的检索基础设施。

### 4. 资源与算力

*   论文未详细说明实验运行所使用GPU的具体型号、数量和训练时长。
*   **计算开销分析**：论文从理论上对比了流水线的并行延迟，指出CQC-RAG在默认配置（\( N=4 \)）下需6次生成调用（1次重写+5次并行推理）和1次批量预填充评估，整体延迟与DMQR-RAG相近，远低于SPARC-RAG的迭代多代理循环。模型的并行推理和无需自回归生成的预填充评估是控制开销的关键设计。

### 5. 实验数量与充分性

*   **实验规模充足**：论文进行了全面的实验验证。
    *   **主实验**：在4个不同复杂度的数据集上系统对比了7种方法，并报告了5个随机种子的均值和标准差，确保了结果的统计稳健性。
    *   **消融实验**：系统移除或替换了三个阶段的各个组件（重写、查询特定排序、证据锚定协议、跨查询评估、独立评估器），分别验证其贡献。
    *   **选择策略分析**：固定候选池，对比了多数投票、概率、困惑度、单查询置信度等6种不同选择策略，证明跨查询一致性评分的优越性。
    *   **参数敏感性分析**：分析了改写查询数量 \( N \) 从2到10对性能的影响，确定了 \( N=4 \) 的最优折衷。
    *   **组件质量分析**：量化评估了重写查询的语义相似度（>0.93）和句法多样性。
    *   **评估器模型分析**：验证了模型解耦的必要性及对不同独立评估器的鲁棒性。
*   **评估客观公平**：所有对比方法均在统一、公平的推理环境和检索设置下进行，主要结果的报告遵循了严格的统计学标准，归因分析逻辑清晰（如指出CQC-RAG和DMQR-RAG在HotpotQA上的性能差异源于数据集特性）。

### 6. 论文的主要结论与发现

*   CQC-RAG在三个数据集（TriviaQA、PopQA、MuSiQue）上均取得了领先的综合性能。在TriviaQA和MuSiQue上，其EM分数分别比最强的多查询基线高出4.76和9.12个百分点，验证了其有效性。
*   跨查询一致性假设得到验证。与基于解码随机性的方法（如Self-Certainty）不同，CQC-RAG能够利用跨查询的置信度稳定性来有效识别和过滤噪声引起的幻觉，尤其是在多跳推理场景下优势巨大（MuSiQue上EM领先+23.22个百分点）。
*   性能提升并非源于扩展检索覆盖。CQC-RAG与DMQR-RAG共享相同的检索基础设施且未增加文档数量，增益完全归功于跨查询一致性评估机制。
*   框架中的“证据锚定协议”对于确保生成答案的忠实度至关重要。
*   使用独立于推理模型的评估器对于规避“自我增强偏差”、保证跨查询评估的辨别力是必要的。

### 7. 优点

*   **思想新颖**：提出了“跨查询一致性”这一核心理念，将答案评估从传统的单一视角转向多视角一致性验证，为鲁棒RAG提供了新范式。
*   **设计巧妙**：
    *   **“免费”的多样性注入**：通过重写查询和重排序在同一文档池上构建差异化上下文，避免了额外的检索开销。
    *   **高效的自我评估**：基于logits的确定性置信度估计，既消除了生成随机性，又通过自适应惩罚系数 \( \lambda(\mu) \) 巧妙地平衡了均值与方差的影响。
*   **实验扎实**：实验设计全面，涵盖了单跳/多跳数据集，对比了各类基线方法，并通过详尽的消融、分析和策略对比实验，对每一处设计选择都提供了坚实的经验证据。
*   **可复现性强**：提供了开源代码，并详细报告了实现细节和实验设置。

### 8. 不足与局限

*   **参数有限分析**：虽然内容详尽，但若能加入对推理量大的场景（如长文档上下文）延迟的实证分析，论证会更完整。
*   **骨干模型局限**：实验仅在8B参数级别的模型（Qwen3-8B, Mistral-7B）上进行，尚不清楚该方法在更大规模或不同架构的模型上是否具有同样的效果和计算效率。
*   **依赖特定能力的模型**：方法的效果依赖于LLM的查询重写能力和重排序器的准确性。如果基础模型在这些子任务上表现不佳，CQC-RAG的性能可能会受到影响。例如，在HotpotQA这类依赖标题匹配的数据集上，由于重写主要改变内容级句法，其效果就不如人意。
*   **应用范围的隐性假设**：跨查询一致性假设的前提是，正确答案的证据不依赖于查询的句法形式。对于某些需要特定关键词触发才有效的知识，或者答案本身就具有主观性或多元性的任务，该框架可能不直接适用。

（完）
