---
title: "Stellar: Scalable Multimodal Document Retrieval for Natural Language Queries"
title_zh: Stellar：面向自然语言查询的可扩展多模态文档检索
authors: "Yuxiang Guo, Zhonghao Hu, Yuren Mao, Yuhang Liu, Congcong Ge, Xiaolu Zhang, Jun Zhou, Yunjun Gao"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.19960v1"
tags: ["query:rag-llm"]
score: 9.0
evidence: 通过磁盘存储令牌级嵌入实现可扩展多模态文档检索
tldr: 多模态文档检索面临内存开销大、可扩展性差的问题。Stellar框架通过词汇过滤和磁盘支持的后期交互，将token嵌入存于磁盘，仅加载少量候选，显著降低内存和延迟。在多个基准测试中，内存和查询延迟降低1-2个数量级，且检索效果不减。该方法为大规模多模态RAG系统提供了高效实用的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19960v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19960v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 821, \"height\": 418, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19960v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1865, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19960v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 890, \"height\": 333, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19960v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 891, \"height\": 342, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19960v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 850, \"height\": 359, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19960v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 859, \"height\": 701, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19960v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 863, \"height\": 330, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19960v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 922, \"height\": 340, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19960v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1832, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19960v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1882, \"height\": 523, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19960v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1755, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19960v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1738, \"height\": 208, \"label\": \"Table\"}]"
motivation: 现有多模态检索方法使用多向量后期交互，虽有效但内存开销极大，难以扩展至大规模应用。
method: Stellar利用微调MLLM生成词汇表示进行过滤，结合基于平衡聚类的磁盘存储布局和动态加载机制，实现高效后期交互。
result: 实验表明，Stellar在四个基准和一个新大规模数据集上，内存开销和查询延迟降低1-2个数量级，同时保持与现有方法相当的检索效果。
conclusion: Stellar为多模态文档检索提供了一种内存高效、可扩展的解决方案，显著降低资源需求而不牺牲精度，对RAG系统实际部署具有重要意义。
---

## 摘要
多模态文档检索——从大型语料库中选出与自然语言查询最相关的多模态文档——在检索增强生成（RAG）系统中扮演着关键角色。现有最优方法使用多个标记级嵌入表示每个文档和查询，并采用后期交互以实现高有效性。然而，这种多向量表示在检索过程中造成大量内存开销，导致可扩展性差，阻碍了实际部署。本文提出Stellar，一种可扩展的多模态文档检索框架，它将标记级文档嵌入存储在磁盘上，仅加载少量候选嵌入到内存中进行后期交互。Stellar包含两个关键组件：(i) 基于词汇表示的过滤（LRF），微调多模态大语言模型（MLLM）作为稀疏编码器生成高质量词汇表示，实现高效且有效的文档过滤，显著减少候选集；(ii) 基于磁盘的高效后期交互（DLI），在平衡聚类算法指导下设计磁盘上的标记嵌入存储布局，并利用简单而有效的成本模型动态加载必要的标记嵌入到内存中。在四个真实基准数据集和一个新提出的大规模数据集上的广泛实验表明，与现有方法相比，Stellar在保持检索有效性的同时，将内存开销和查询延迟降低了1-2个数量级。

## Abstract
Multimodal document retrieval--selecting the most relevant multimodal document from a large corpus to answer a natural language query--plays an essential role in Retrieval-Augmented Generation (RAG) systems. State-of-the-art methods represent each document and query with multiple token-level embeddings and use late interaction to achieve high effectiveness. However, such multi-vector representations incur substantial memory overhead during retrieval, leading to poor scalability and hindering real-world deployment. In this paper, we present Stellar, a scalable multimodal document retrieval framework that stores token-level document embeddings on disk and loads only a small set of candidate embeddings into memory for late interaction. Stellar comprises two key components: (i) Lexical Representation-based Filtering (LRF), which fine-tunes a Multimodal Large Language Model (MLLM) as a sparse encoder to produce high-quality lexical representations, enabling efficient and effective document filtering to significantly reduce the candidate set; (ii) Efficient Disk-backed Late Interaction (DLI), which designs an on-disk token embedding storage layout guided by a balanced clustering algorithm, and dynamically loads only the necessary token embeddings into memory using a simple yet effective cost model. Extensive experiments on four real-world benchmarks and a newly presented large-scale dataset demonstrate that Stellar reduces memory overhead and query latency by 1-2 orders of magnitude compared to existing methods without compromising retrieval effectiveness.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究背景**：检索增强生成（RAG）系统依赖多模态文档检索，以从包含文本、图表等视觉元素的多模态文档中定位与用户自然语言查询相关的页面。
- **现有问题**：当前最优的多向量后期交互方法（如 ColPali）虽能通过标记级嵌入与 MaxSim 机制实现高精度检索，但在检索阶段需要将所有文档的标记级嵌入全部驻留在内存中，导致极高的内存开销（例如 240 万文档估计需要 800 GB 以上内存）和查询延迟，严重限制其在大规模场景下的可扩展性与实际部署。
- **核心动机**：放弃“将嵌入压缩驻留内存”的思路，转向“将原始标记嵌入存储在廉价磁盘上，在线检索时仅加载少量相关嵌入到内存进行后期交互”，从根本上降低内存占用，同时保持检索有效性。

### 2. 论文提出的方法论（核心思想、关键技术细节、公式或算法流程）
#### 2.1 整体框架 Stellar
Stellar 由两个离线构建和在线执行的关键组件构成：

| 阶段 | 组件 | 功能 |
|------|------|------|
| 首次过滤 | **基于词汇表示的过滤 (LRF)** | 利用微调后的多模态大语言模型（MLLM）将文档和查询映射到统一稀疏词汇空间，通过内存倒排索引快速筛选出 top‑k₁ 候选文档 |
| 精细排序 | **基于磁盘的高效后期交互 (DLI)** | 仅将候选文档的标记级嵌入从磁盘加载到内存，执行后期交互计算并融合稀疏分数，输出最终排序结果 |

#### 2.2 词汇表示学习与过滤 (LRF)
- **统一词汇投影**：复用 MLLM 的预训练语言建模头 **f<sub>head</sub>** 作为投影层，将文档视觉令牌和查询文本令牌的最后一层隐藏状态映射为词汇空间上的权重向量。
- **稀疏化**：对每个令牌的分数应用 `ReLU` 去负值，再进行 `log(1+ReLU(w))` 变换，最后沿令牌维度做 `max pooling`，得到文档的稀疏表示 `enc_spa(d) ∈ ℝ^(|V|)`（|V| 为词表大小，约 15 万维）。
- **训练目标**：在成对对比学习（InfoNCE）损失基础上，引入 FLOPs 正则项（对文档和查询表示的非零维度计数进行约束），使表示高度稀疏（非零维度平均约 200，稀疏率 ≈99.87%）。
- **过滤机制**：离线构建倒排索引，在线时仅遍历查询非零维度对应的 posting list 进行稀疏评分 `score_spa`，快速返回 top‑k₁ 候选文档。

#### 2.3 基于磁盘的后期交互 (DLI)
- **多向量表示**：复用同一个 MLLM 骨架，额外添加线性投影层为每个令牌生成低维稠密嵌入 `e_i = W·h_i + b`，文档和查询分别表示为嵌入集合。
- **平衡聚类存储布局 (BCA)**：
  - 利用 LR 阶段的稀疏表示对文档进行聚类（种子数 K = ⌈|D|/s_exp⌉，s_exp 为目标簇大小）。
  - 对大簇递归执行 k‑means 分裂，直到所有簇大小 ≤ s_exp。
  - 合并过小簇（大小 < s_min）并将其文档按相似度重新分配给幸存簇。
  - 同一簇内文档的标记级嵌入在磁盘上连续存储为一个块。
- **两级内存索引**：文档级索引记录每个文档所属块、令牌数、偏移与长度；块级索引记录块内文档列表和向量总数。
- **成本感知加载**：对每个命中的磁盘块，基于 **顺序读/随机读** 吞吐率建立简单成本模型，估算两种加载策略的时间：
  - **FBL（整块加载）**：`T_FBL = (N_total × Vdim × B_float)/R_seq`
  - **SVL（仅加载候选向量）**：`T_SVL = (N_req × Vdim × B_float)/R_rand`
  动态选择估计成本更低的策略加载标记嵌入。
- **分数融合**：将在线计算的后期交互稠密分数 `score_mul` 与第一阶段稀疏分数 `score_spa` 经 z‑归一化后线性融合（加权系数 α = 0.3），得到最终排序分数 `score_fus`。

### 3. 实验设计（数据集、基准与对比方法）
- **数据集**：
  - 四个现有中小规模基准：DocVQA（工业文档）、InfoVQA（信息图）、ArXivQA（学术图表）、PlotQA（科学图表）。
  - 新构建的大规模数据集 **LargeDoc**：基于 Docmatix 筛选 94 对高质量查询‑文档，并与约 40 万干扰文档混合，经自动 + 人工清洗去除假阴性，最终包含 400,000 个文档和 94 个测试查询。
- **对比方法**：
  - 文本中心：BM25、BGE‑Large、gte‑Qwen2（基于 OCR 提取文本）。
  - 视觉中心单向量：DSE、VisRet、Bi‑Qwen（均以 MLLM 编码为单向量）。
  - 视觉中心多向量：ColPali（精确后期交互）、QColPali（基于 PLAID 的量化近似）。
- **评估指标**：Recall@1/10、MRR@10，同时测量峰值内存占用和查询延迟。
- **公平性措施**：所有视觉方法统一使用 Qwen2‑VL‑2B 作为骨干，在相同的混合训练集上微调一个 epoch，均使用 LoRA rank=32。

### 4. 资源与算力
- 论文未明确给出 GPU 型号、数量及具体训练时长。
- 仅提及实验服务器配备 **Intel Xeon Silver 4316 CPU @2.30GHz、256 GB 内存**，在线检索全在 CPU 上执行。
- 训练采用 LoRA 微调，对算力要求较低，但缺乏详细的算力配置说明，这一点可能影响可复现性的评估。

### 5. 实验数量与充分性
- 实验覆盖 **5 个不同领域、不同规模的数据集**（表 II），测试了约 10 种对比方法，评估了有效性与效率（内存、延迟）。
- 针对 Stellar 的两大组件进行了详细的消融与验证：
  - LRF 与 DSE、QColPali 过滤器的召回对比（图 7），以及稀疏表示的非零维度统计（图 5）。
  - DLI 阶段不同聚类策略（BCA vs. 朴素聚类）与不同加载策略（FBL、SVL）的消融（表 IV）。
  - 分数融合的消融与 α 参数的敏感性分析（表 V、图 8）。
- 可扩展性分析：在 LargeDoc 上模拟从 0.1× 到 4× 的数据规模，观察内存与延迟的增长趋势（图 6）。
- 总体实验设计系统、对比全面、消融充分，并额外贡献了大规模基准数据集，实验客观性强。

### 6. 论文的主要结论与发现
- Stellar 在所有数据集的 R@1 上达到或超越当前最优水平（ColPali），且无需后期交互评估全量文档。
- 内存占用降低 1~2 个数量级：在 40 万文档规模上仅需 0.96 GB，而 ColPali 为 147 GB，QColPali 约 13.7 GB。
- 查询延迟大幅降低：Stellar 的延迟主要在 40~110 ms 范围内，ColPali 在大型数据集上高达约 42 秒。
- 词汇过滤（LRF）不仅速度快、内存小（高稀疏度），且召回率优于基于稠密向量的过滤（DSE 或质心过滤），并避免成为内存瓶颈。
- 平衡聚类与成本感知加载策略显著降低了磁盘 I/O 负担，比单纯的整块加载或随机向量加载效率更高。
- 稀疏‑稠密分数融合进一步提升了精度，α≈0.3 时效果最佳。

### 7. 优点（方法或实验设计上的亮点）
- **存算分离式可扩展设计**：将大量标记嵌入存于磁盘，仅按需加载，从根本上缓解内存膨胀，对实际部署极具价值。
- **复用 MLLM 预训练头实现高效稀疏编码**：轻量且与后端模型共享，避免额外存储代价，且稀疏率极高，过滤阶段几乎无延迟。
- **平衡聚类磁盘布局**：利用第一阶段的稀疏表征指导语义聚类，兼顾 I/O 效率和块大小均衡，是一种简单而精巧的存储优化。
- **成本感知动态加载**：用少量测量参数构建成本模型，自适应选择整块读取或随机读取，工程实用性强。
- **新构建的大规模基准 LargeDoc**：填补了多模态文档检索领域大型公开数据集的空白，并经过仔细假阴性清理，增强社区实验可复现性。
- **实验全面**：涵盖多个领域、多尺度数据，同时对比文本/视觉单向量/多向量三大类方法，消融与效率分析深入。

### 8. 不足与局限
- **离线训练算力信息缺失**：未说明训练所用的 GPU 资源与时长，给方法复现带来不确定性。
- **多文档检索未覆盖**：当前设定仅针对答案位于单个文档页面的场景，未扩展至跨多个多模态文档的检索任务。
- **CPU 在线检索假设**：所有实验在 CPU 上执行，未讨论利用 GPU 加速后期交互的可能性，可能低估了在 GPU 可用时的延迟差距。
- **索引构建与更新成本未详述**：平衡聚类算法需要在全量稀疏表示上运行 k‑means，对于动态更新的语料，索引维护成本未知。
- **成本模型的简化**：只使用顺序/随机读吞吐率两个参数，未考虑 SSD 内部并行性、I/O 队列深度等更复杂的存储特性，可能在极端负载下偏离实际。
- **特定骨干依赖**：实验统一基于 Qwen2‑VL‑2B，更换其他 MLLM 时的泛化性未验证。

（完）
