---
title: "Mind the Gap: Bridging Behavioral Silos with LLMs in Multi-Vertical Recommendations"
title_zh: 弥合差距：利用大型语言模型在多垂直领域推荐中连接行为孤岛
authors: "Nimesh Sinha, Raghav Saboo, Martin Wang, Sudeep Das"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06779v1"
tags: ["query:rag-llm"]
score: 8.0
evidence: 采用分层检索增强生成（RAG）管线从用户数据中提取特征用于推荐
tldr: 多垂直电商平台如DoorDash的新垂直领域（杂货、零售）面临用户冷启动问题，因行为数据稀疏导致推荐难。本文引入基于LLM的生成式推理框架，利用层次化检索增强生成管道从数据丰富的餐厅订单历史中提取多级分类特征，编码用户的长期跨垂直偏好和短期意图。这些特征被集成到多任务学习排序模型中，通过离线和在线实验验证，显著提升了新兴业务垂直领域的个性化推荐和用户参与度，有效弥合了行为数据鸿沟。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06779v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1432, \"height\": 317, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06779v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 862, \"height\": 540, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06779v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 868, \"height\": 457, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06779v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 863, \"height\": 467, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06779v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1846, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06779v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1848, \"height\": 395, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06779v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1280, \"height\": 177, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06779v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1278, \"height\": 178, \"label\": \"Table\"}]"
motivation: 多垂直电商的新垂直领域缺乏用户行为数据，冷启动问题严重，限制个性化推荐效果。
method: 利用LLM和层次化RAG管道，从用户餐厅订单历史中生成多级分类特征，编码跨垂直偏好与短期意图，并集成到MTL排序模型。
result: 离线和在线实验表明，该方法显著提升了新兴垂直领域的推荐个性化与用户参与度，有效弥合行为数据鸿沟。
conclusion: 通过LLM驱动的知识迁移，可将数据丰富垂直的行为信号有效迁移至数据稀疏垂直，解决冷启动，为多垂直推荐提供新范式。
---

## 摘要
在多垂直领域的电商平台（如DoorDash）中，相对较新的产品垂类（例如杂货和零售）为个性化创新带来了重要机遇。一个关键的挑战在于解决用户的“冷启动”问题。本文提出了一种新颖的框架，通过将从数据丰富的垂类（如DoorDash的餐饮）向数据稀疏的垂类迁移知识来提升推荐质量。我们利用大型语言模型（LLMs）进行生成式推理，合成包含潜在用户偏好的稀疏高维特征。具体来说，我们采用了一个分层的检索增强生成（RAG）流程，从用户的餐厅订单历史和搜索查询中提取多级分类特征。这些生成的特征编码了长期的跨垂类偏好和短期意图，并被集成到一个在线的多任务学习（MTL）排序模型中。通过广泛的离线和在线评估，我们证明该方法显著改善了新兴业务垂类的个性化和用户参与度，有效地弥合了行为数据差距。

## Abstract
In multi-vertical e-commerce platforms like DoorDash, relatively newer product verticals such as grocery and retail present a significant opportunity for personalization innovation. A key challenge lies in solving the "cold start" problem for users. This paper introduces a novel framework for enhancing recommendation quality by transferring knowledge from data-rich verticals (e.g., restaurants at DoorDash) to data-sparse ones. We leverage Large Language Models (LLMs) to perform generative inference, synthesizing sparse, high-dimensional features that encapsulate latent user affinities. Specifically, we employ a hierarchical Retrieval-Augmented Generation (RAG) pipeline to derive multi-level taxonomic features from user restaurant order histories and search queries. These generated features, encoding both long-term cross-vertical preferences and short-term intent, are integrated into a production Multi-Task Learning (MTL) ranking model. We demonstrate through extensive offline and online evaluation that this approach significantly improves personalization and engagement in emerging business verticals, effectively bridging the behavioral data gap.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将使用中文，以Markdown的形式，对《Mind the Gap: Bridging Behavioral Silos with LLMs in Multi-Vertical Recommendations》这篇论文进行结构化、深入且客观的总结。

### 1. 论文的核心问题与整体含义
本论文旨在解决**多垂直领域电商平台中的用户冷启动问题**。
*   **研究背景与动机**：像DoorDash这样的平台，除了核心业务（如餐饮）外，会不断拓展到杂货、零售等新垂类。新垂类面临**行为数据稀疏**的挑战，导致难以对无历史交互的用户进行精准个性化推荐。而平台在成熟的数据富集垂类（如餐饮）中积累了大量的用户行为数据。
*   **核心思想**：论文提出**跨越“行为孤岛”进行知识迁移**。利用用户在数据富集垂类（餐饮）的历史行为信号，通过LLM的语义理解与生成能力，推断其在新垂类（杂货、零售）中的潜在偏好，从而在不依赖新垂类历史数据的情况下实现有效个性化，弥合数据鸿沟。

### 2. 论文提出的方法论
论文的核心方法是利用**层次化检索增强生成（Hierarchical RAG）** 流程，从用户历史行为中合成偏好特征，并注入到排序模型中。
*   **特征生成 (`Generative Feature Synthesis`)**:
    *   **两类合成特征**:
        1.  **长期跨垂类偏好特征 (`Long-Term Cross-Vertical Affinity Profile`)**: 从用户的**累计餐饮订单历史**中推断而来。
        2.  **短期意图特征 (`Short-Term Intent Profile`)**: 从用户的**近期的搜索查询**行为中推断而来。
    *   **层次化RAG流程 (`Hierarchical RAG Pipeline`)**:
        *   **输入信号**: 按时间排序的用户餐饮订单（餐厅名+菜品名）和搜索查询词。
        *   **流程机制**: 采用**级联推理 (`Cascaded Inference`)**。首先让LLM识别用户高层次的（L1、L2）品类偏好。然后，这些高置信度的初步分类结果将作为约束，用于指导后续更细粒度（L3）的品类检索。此迭代优化策略旨在提升生成特征的精确性，防止模型生成似是而非的错误子类。
        *   **输出目标**: 将输入信号映射到平台内部的四级分类体系（L1–L4，例如 L1: `Dairy & Eggs` -> L2: `Cheese` -> L3: `Hard Cheeses` -> L4: `Cheddar`）。由于L1过于宽泛，L4过于稀疏，该方法主要集中于生成**L2和L3级别的品类偏好特征**。
*   **工程优化与质量控制 (`Prompt Engineering and Inference Control`)**:
    *   **提示词设计**: 精心设计的提示词包含角色设定（推荐引擎）、操作规则（仅使用提供的分类、大小写匹配）、分类信息（允许的L3分类列表）和用户历史。提示词要求模型为每个生成的偏好输出一个**置信度分数 (`confidence score`)**。
    *   **质量控制**: 通过设置**低推理温度（0.1）** 确保输出确定性，并要求模型**仅返回置信度高于0.8的分类**，作为一种自我修正机制，过滤低质量的联想。
*   **成本优化 (`Cost Optimization`)**:
    *   **模型选择**: 通过成本-性能分析，选择了性价比更高的`GPT-4o-mini`。
    *   **提示词缓存 (`Prompt-Caching`)**: 将包含指令和分类体系的静态部分缓存，每次推理只附加动态的用户历史，结合按需特征生成策略，将**计算成本降低了约80%**。
*   **特征集成 (`Feature Integration`)**:
    *   生成的离散分类特征通过**嵌入表 (`Embedding Table`)** 映射为稠密向量，并用**平均池化 (`Mean Pooling`)** 聚合成固定大小的表示。
    *   聚合后的用户偏好向量`u_LLM`与现有用户特征`u_eng`和物品特征`i_eng`拼合，形成最终的用户表示`u_aug = [u_eng; u_LLM]`。
    *   该增强的表示被输入到**多任务学习 (`MTL`) 排序模型**的共享底层网络（`MLP trunk`）中，用于联合优化点击、加购、下单等多个业务目标。其总损失函数为各任务损失的加权和：\(\mathcal{L} = \sum_{t=1}^{T} \alpha_t \mathcal{L}_t (\hat{y}_t, y_t)\)。

### 3. 实验设计
*   **数据集**: 内部DoorDash平台数据，训练基于**三个月**的用户行为数据，离线评估在一个**15天**的留出集上进行。
*   **基准模型 (`Baseline`)**: 在线的生产环境MTL排序模型，该模型仅使用用户历史交互特征和物品属性特征，未使用LLM生成的特征。
*   **对比方法 (`Proposed Model`)**: 与基准模型架构完全相同，但输入特征中**加入了LLM生成的特征**。
*   **评估指标**: `AUC-ROC`（衡量分类性能）和`MRR`（衡量排序质量）。
*   **用户分群分析 (`Cohort Analysis`)**:
    *   **总体用户**: 全量评估用户。
    *   **冷启动用户 (`Cold-start consumers`)**: 新进入非餐饮垂类的用户。
    *   **高活用户 (`Power consumers`)**: 在非餐饮垂类高度活跃的用户。
*   **在线评估**: 进行了**影子部署 (`Shadow Deployment`)**，即在生产环境中对模型进行线上流量比对，但不影响真实用户的体验。

### 4. 资源与算力
*   文中明确提出使用了`GPT-4o-mini`模型进行特征生成，并提及通过提示词缓存和按需生成策略，将**计算成本降低了约80%**。
*   然而，论文**没有**报告具体的硬件资源使用情况（如GPU型号、数量）、模型训练总时长或具体总花费金额。

### 5. 实验数量与充分性
论文实验数量相对有限，但设计具有针对性，覆盖了核心验证点。
*   **实验数量**:
    1.  **定性评估**: 人评和LLM-as-a-judge两种方式的特征质量评估。
    2.  **离线定量评估**: 在总体、冷启动、高活三个用户分群上对比了`Baseline`和`Proposed Model`。
    3.  **在线影子评估**: 在总体用户上对比了两个模型的线上性能。
*   **充分性与客观性**:
    *   **优点**: 实验设计逻辑清晰，离在线相结合，并针对不同价值用户群进行了细分分析，验证了方法的适用性。对比模型结构一致，变量控制严格，相对公平。
    *   **不足**: 缺少消融实验来剥离两类特征（长期 vs. 短期）的各自贡献，也未对不同LLM模型或不同尺寸的模型进行更细致的性能对比。

### 6. 论文的主要结论与发现
*   **总体有效性**: LLM增强模型在离线和在线场景下均显著优于基线。离线相对提升为**AUC-ROC +4.4%**, **MRR +4.8%**；在线相对提升为**AUC-ROC +4.3%**, **MRR +3.2%**。
*   **分工明确**: 两种LLM特征对不同用户群体作用显著不同。基于**餐饮订单的长期偏好特征**对**冷启动用户**帮助最大；基于**搜索查询的短期意图特征**对**高活用户**提升最明显。
*   **知识可迁移性**: 实验有力地证明，用户在一个领域的历史口味偏好（如对特定菜系的喜好）可以通过LLM的语义桥接，有效迁移到另一个看似不相关的产品领域，从而解决冷启动问题。

### 7. 优点
*   **创新性思路**: 提出了将LLM作为“语义桥梁”来跨垂类迁移用户偏好的新颖应用，有效应对了多场景下的冷启动难题。
*   **工程落地性强**: 方法是完整的端到端工业级方案，涵盖了特征生成（层次化RAG）、质量保障（置信度过滤）、成本优化（模型选择、缓存）和线上集成（MTL特征增强、影子部署）的全链路。
*   **严谨的质控设计**: 通过低温度、置信度阈值和级联的层次化RAG流程，有效减少了LLM的幻觉，保障了生成特征的可靠性，并通过人评和LLM-as-a-judge双重验证。
*   **业务价值导向**: 通过详细的用户分群分析，清晰展示了不同特征对不同用户群体的业务价值，结论具有很强的指导意义。

### 8. 不足与局限
*   **实验维度不够丰富**: 缺少关键的消融实验，无法量化长期偏好和短期意图两类特征各自的独立贡献，也无法分析移除RAG中层次化约束的影响。
*   **评估指标的局限性**: 仅采用AUC和MRR，缺少线上A/B测试的**核心业务指标**（如实际转化率、GMV提升），其影子测试的模型效果与实际业务收益之间的关系尚不明确。
*   **对新LLM的高度依赖**: 方法的效果严重依赖LLM的能力和平台的分类体系，其成本对高频更新的场景依然可能构成挑战，且迁移到其他平台时需要重新进行提示词和分类体系的适配。
*   **潜在偏差风险**: LLM可能从餐饮数据中推断出带有刻板印象的关联（例如，从特定族裔的菜品推断其对某类杂货的偏好），可能引入推荐系统的公平性风险，文中未对此进行讨论。

（完）
