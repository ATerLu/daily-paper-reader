---
title: "Navigating Unreliable Parametric and Contextual Knowledge: Explicit Knowledge Conflict Resolution for LLM Inference"
title_zh: 驾驭不可靠的参数化知识与上下文知识：面向大语言模型推理的显式知识冲突消解
authors: "Huang Peng, Jiuyang Tang, Weixin Zeng, Hao Xu, Xiang Zhao"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20245v1"
tags: ["query:rag-llm"]
score: 8.0
evidence: 解决集成外部上下文的LLM中的知识冲突，提出显式冲突解决机制以提升推理可靠性；与结合检索的LLM直接相关。
tldr: 大型语言模型在利用参数知识和提示上下文时，常面临内部知识与外部信息间、多源上下文间的知识冲突。现有方法多假定一方可靠，回避而非主动消解不一致。本文提出基于多智能体推理的MACR框架，先通过修正语义熵评估模型答案置信度，自适应选择外化模型内部知识或检索外部知识以构建推理基础；随后运用三专职智能体分别归纳显式规则、识别潜在冲突、消解跨上下文矛盾，实现显式冲突解决。实验表明，MACR在多个知识冲突基准上显著优于现有方法，并提供可解释的冲突消解路径，为可靠的知识增强推理提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20245v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 891, \"height\": 676, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20245v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1304, \"height\": 810, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20245v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 732, \"height\": 547, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20245v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1309, \"height\": 557, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20245v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 917, \"height\": 236, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20245v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 497, \"height\": 217, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20245v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 984, \"height\": 606, \"label\": \"Table\"}]"
motivation: 现有知识冲突处理方法假设一方可靠而回避不一致，无法应对模型与上下文均可能出错的复杂场景。
method: 提出MACR框架，采用修正语义熵进行置信度评估，自适应外化或检索知识，并通过归纳式三智能体推理显式消解冲突。
result: MACR在多个知识冲突基准上大幅超越现有方法，并输出可解释的冲突消解过程，验证了显式冲突解决的有效性。
conclusion: MACR超越了传统二选一范式，通过显式冲突消解机制增强了LLM知识推理的鲁棒性和可解释性，为更可靠的AI应用奠定了基础。
---

## 摘要
大型语言模型（LLMs）凭借广泛的参数化知识和上下文学习能力，得以融入输入提示中的外部信息，在各类语言任务中表现出色。然而，外部知识的整合可能引发冲突，不仅体现在模型内部参数知识与外部信息之间，也存在于多段外部上下文之间。现有方法通常假设模型或所提供的上下文是可靠的，忽视了两者均可能含错的情况，并通过优先选择某一来源来避开冲突，而非主动消解不一致性。为克服这些局限，我们提出了一种名为MACR的新框架，用于大模型的知识冲突消解，它超越了传统的二元选择范式，引入了基于多智能体推理的显式冲突消解机制。具体而言，我们首先提出一种自适应知识评估与检索方法，采用改进的语义熵度量来量化大模型对其给定查询答案的置信度。基于该置信度估计，MACR或则将模型内部知识外化为文本表征，或则在内部知识不足时检索相关外部知识，生成用于后续推理的基础上下文。接着，我们引入一个归纳多智能体推理框架，其包含三个专用智能体，分别负责归纳显式规则、分析潜在冲突，并消解所有可用上下文之间的不一致性。实验结果表明，MACR在多个基准测试中显著优于现有最优基线方法，同时提供了可解释的显式冲突消解方案。

## Abstract
Large language models (LLMs) have achieved strong performance across a wide range of language-based tasks by leveraging both extensive parametric knowledge and in-context learning ability, enabling them to incorporate external information provided in the input prompt. However, the integration of external knowledge can introduce conflicts, not only between the model's internal parametric knowledge and the external information, but also among multiple pieces of external contexts. Existing approaches typically assume that either the model or the provided context is reliable, overlooking the possibility that both sources may contain errors, and avoid conflicts by privileging one source over the other, rather than actively resolving inconsistencies. To address these limitations, we propose a novel framework MACR for LLM knowledge conflict resolution that moves beyond the conventional binary choice paradigm and incorporates an explicit conflict-resolution mechanism based on a multi-agent reasoning approach. Specifically, we first propose an adaptive knowledge assessment and retrieval approach that employs a modified semantic entropy measure to quantify an LLM's confidence in its answer to a given query. Based on this confidence estimation, MACR either externalizes the model's internal knowledge as textual representations or retrieves relevant external knowledge when internal knowledge is insufficient, generating basic contexts for subsequent reasoning. Then we introduce an inductive multi-agent reasoning framework with three specialized agents that, respectively, induce explicit rules, analyze potential conflicts, and resolve inconsistencies across all available contexts. Empirical results demonstrate that MACR significantly outperforms state-of-the-art baselines across benchmarks, while also providing interpretable resolutions of explicit conflicts.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：大型语言模型（LLM）在结合外部上下文（如检索增强生成）时，会面临 **参数知识与上下文知识之间** 以及 **多段上下文之间** 的知识冲突。例如，模型内部记忆的过时事实与检索到的新信息相矛盾，或不同来源的上下文彼此不一致。
- **现有局限**：当前方法大多假设模型或上下文中的某一方绝对可靠，从而退化为一个 **二元选择（either/or）问题**——要么信任内部知识，要么信任外部上下文。这种做法 **回避了显式的冲突分析**，无法处理 **双方均可能出错** 的现实复杂场景。
- **本文目标**：提出 MACR 框架，通过 **显式冲突消解** 机制，在不预先假定任何信息来源可靠的前提下，主动识别、分析并消解来自内部知识与外部上下文的所有不一致，从而生成更准确、更具可解释性的答案。

### 2. 方法论
MACR 框架由三个顺序模块构成：**知识评估**、**知识检索**、**归纳多智能体推理**。

- **知识评估**  
  采用一种 **改进的语义熵** 衡量模型对查询的置信度。  
  - 基础语义熵 \(H(q) = -\sum_i P(a_i|q) \log \sum_j \exp(f(a_i, a_j))\)  
  - **改进**：将答案与查询的相关性 \(\text{sim}(a_i, q)\) 融入 pairwise 相似度计算，得到 \(H_{\text{sem}}(q)\)，确保只有在回答既内部一致又问题相关时才呈现低熵（高置信度）。  
  - 进一步在提示中加入 **时间信息** 和 **实体消歧信息** 以缓解时态错配和主语模糊对置信度的干扰。

- **知识检索**  
  依据置信度得分 \(C(q)\) 与阈值 \(\tau\) 比较，分两路生成基础上下文 \(C_0\)：  
  - **高置信（\(C(q) < \tau\)）**：指示模型 **外化内部参数知识**，生成结构化的内部上下文 \(C_{\text{int}}\)，并基于此给出初始答案 \(A_{\text{init}}\)。  
  - **低置信（\(C(q) \ge \tau\)）**：触发外部检索，从外部语料库中获取 \(C_{\text{ext}}\)，并仅依据该外部证据生成 \(A_{\text{init}}\)，避免幻觉。

- **归纳多智能体推理**  
  由三个专职智能体协作完成冲突消解：  
  - **观察者（Observer）**：在线下阶段从训练数据中 **归纳候选规则**（类型、条件、解决方式），并在验证集上依据 **覆盖率** 和 **支持度** 进行筛选，形成高质量规则库 \(R_{\text{final}}\)。  
  - **分析者（Analyzer）**：在线推理时，基于每个上下文生成独立答案，进行 **答案级冲突检测**，若存在冲突，则回溯至原文提取 **冲突片段** 并标注冲突类型，形成冲突三元组 \(\psi\)。  
  - **推理者（Reasoner）**：对每个 \(\psi\)，查询规则库中匹配的规则，根据规则的条件和解决方式做出 **局部裁决**；若未找到规则，则触发回退机制并反馈给观察者更新规则。最终汇总所有裁决，合成最终答案 \(A_{\text{final}}\) 与解释 \(E\)。

### 3. 实验设计
- **数据集与场景**：使用三个专门针对知识冲突的基准数据集：  
  - **ConflictBank**：包含错误信息冲突、时间冲突、语义冲突等多类型冲突。  
  - **ConFiQA**：评估在存在反事实上下文时模型的真实性。  
  - **MQuAKE**：多跳问答，需要回答与模型预训练知识相矛盾的问题。  
- **评估指标**：精确匹配（EM）和 ROUGE-L。  
- **对比方法**：  
  - **Direct**：无任何上下文，仅靠参数知识直接回答。  
  - **ICL**：将包含噪声的全部上下文直接给模型。  
  - **鲁棒 RAG 方法**：InstructRAG（ICL 版本）、TruthfulRAG。  
  - **动态选择方法**：CK-PLUG，基于置信度在内部答案和上下文答案之间切换。

### 4. 资源与算力
- 论文未明确提及训练或推理所使用的 **GPU 型号、数量及训练时长**。  
- 仅提到在知识检索模块中，当需要外部知识时使用了 **GPT-4o-mini** 作为外部信息来源；计算语义熵时生成 \(k=8\) 个样本，相似度函数使用 ROUGE-L。  
- 所有对比方法均在同一硬件环境下复现，但具体资源规格未说明。

### 5. 实验数量与充分性
- **主实验**：在三个数据集（ConflictBank、ConFiQA、MQuAKE）上，使用 Llama3.1-8B 和 Qwen2.5-7B 两种模型，与 5 种基线方法比较，共 6 组结果。  
- **变体上下文实验**：在 ConflictBank 上，将上下文数量从 3 增至 5（噪声比 50%→75%），测试 MACR、ICL、InstructRAG、TruthfulRAG 的鲁棒性，含 4 种方法 × 3 种设置共 12 组结果。  
- **消融实验**：移除知识评估与检索模块（w/o KA&R）、将多智能体推理替换为思维链（w/ CoT），在 ConflictBank 上对比完整模型，共 3 组。  
- **案例研究**：详细展示时间冲突特例的处理过程。  
- 实验覆盖多模型、多冲突类型、多噪声比例，消融分析剥离了关键模块贡献，比较基准涵盖固定范式与动态选择，整体实验设计 **较为充分、客观且公平**。

### 6. 主要结论与发现
- MACR 在所有数据集和模型上 **一致取得最优性能**，尤其在知识高度缺失的 ConflictBank 上提升显著（如 Llama3.1-8B 上 EM 从最优基线的 0.312 提升至 0.549）。  
- 随着上下文噪声比例增加，MACR 的性能下降 **远小于基线方法**，验证了其归纳推理机制有效过滤噪声、抓取可信信息。  
- 消融实验证实，**知识评估与检索** 和 **归纳多智能体推理** 均为框架的关键贡献，任何一处缺失都会导致性能大幅下滑。  
- 所生成的 **可解释冲突消解说明**，清楚展示了冲突识别、规则应用和最终裁决的逻辑链条，增强了系统的透明度。

### 7. 优点
- **跳出二元假设**：不预设任何一方可靠，能应对内部知识与外部上下文 **同时错误** 的现实情况。  
- **显式与可解释**：通过规则归纳、片段抽取和规则应用生成 **可读的冲突消解解释**，利于审计与信任构建。  
- **自适应机制**：用融入相关性的改进语义熵 **动态评估置信度**，智能选择依赖内部知识还是外部检索。  
- **多智能体协作**：观察者、分析者、推理者的分工使规则学习与冲突消解过程结构化， **可复用、可更新**。  
- **实验验证全面**：覆盖多数据集、多模型、多噪声条件，消融分析明确道出各部件的独立贡献。

### 8. 不足与局限
- **计算效率**：多智能体之间的迭代调用以及反馈循环导致 **频繁 LLM 请求**，推理延迟和计算开销明显高于单次解码的方法。  
- **规则质量依赖模型能力**：离线规则归纳的稳定性和泛化性受底层 LLM 抽象能力的限制，高歧义场景下可能产生 **不稳定的规则**。  
- **实验资源信息缺失**：未报告 GPU 配置、推理时长等资源消耗数据，难以评估实际部署的成本。  
- **应用场景边界**：实验集中在知识冲突基准问答，对于开放域生成或更长篇幅的文本冲突消解尚未涉足，泛化性需进一步检验。

（完）
