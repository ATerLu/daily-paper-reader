---
title: "Ricci-Filtration: Boosting Retrieval-Augmented Generation Reranker to Query-Answer Tasks by Discrete Ricci Flow"
title_zh: Ricci-Filtration：借助离散 Ricci 流增强面向查询-回答任务的检索增强生成重排序器
authors: "Tian Qin, Wei-Min Huang"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15482v1"
tags: ["query:rag-llm"]
score: 10.0
evidence: 提出Ricci-Filtration，使用离散Ricci流增强RAG重排序
tldr: 在检索增强生成（RAG）中，初始检索块常含噪声信息，损害生成质量。为此，提出Ricci-Filtration，将查询与块建模为图，通过离散Ricci曲率和Ricci流评估块的结构重要性，过滤负曲率与高权重的噪声块，再输入重排序器。实验表明，该方法在准确率、精确率、召回率、F1等指标上显著超越多种基线，且在不同架构下保持鲁棒。该工作为RAG引入几何驱动的去噪框架，展示了离散曲率在信息检索优化中的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15482v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1784, \"height\": 432, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15482v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1787, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15482v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 841, \"height\": 640, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15482v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 855, \"height\": 620, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15482v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1757, \"height\": 252, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15482v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1761, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1762, \"height\": 277, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1773, \"height\": 648, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1761, \"height\": 614, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1767, \"height\": 162, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15482v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1125, \"height\": 206, \"label\": \"Table\"}]"
motivation: RAG检索的文档块常含噪声，降低生成答案的准确性，需要设计有效的过滤机制提升查询-回答任务性能。
method: 基于离散Ricci流，将查询与候选块构建为加权图，利用曲率识别结构无关的噪声块，滤除后交给重排序器处理。
result: 在多个数据集上，Ricci-Filtration的精确率、召回率和F1均超越基线，消融实验验证了其在不同设置下的鲁棒性。
conclusion: 几何曲率驱动的去噪方法有效提升了RAG性能，Ricci-Filtration为利用图几何结构优化信息检索提供了新思路。
---

## 摘要
Ricci 流是一种曲率引导的扩散过程，通过收缩高正曲率区域、膨胀负曲率区域来使空间变形。类似地，加权图上的离散 Ricci 流通过收缩具有正 Ricci 曲率的边、拉伸负 Ricci 曲率的边来修改边权重，从而有效增加簇间的分离度。受这两项基石性工作的启发，我们提出了一种基于几何的 RAG 重排序器增强方法，称为 Ricci-Filtration。通过将输入查询和初始检索到的片段建模为一个网络，其中输入查询和片段作为节点，基于嵌入的成对关系定义初始图，Ricci-Filtration 利用离散曲率和 Ricci 流来评估每个片段相对于用户查询的结构重要性。系统首先根据片段相对于查询的几何曲率过滤初始片段；然后，重排序器处理剩余的片段以增强生成性能。我们从理论上证明，归一化的离散 Ricci 流可以通过识别边权重的不同渐近行为来检测社区结构。这支持移除相对于查询节点具有大权重和负 Ricci 曲率的“噪声”文档片段。大量实验证实，Ricci-Filtration 在准确率、精确率、召回率和 F1 分数上优于几种基线重排序方法。此外，消融研究表明，在各种设置下，Ricci-Filtration 总体上优于基线，凸显了该框架在不同架构上的鲁棒性。

## Abstract
Ricci flow is a curvature-guided diffusion process that deforms space by shrinking regions of high positive curvature and expanding those with negative curvature. Similarly, discrete Ricci flow on weighted graphs modifies edge weights by shrinking edges with positive Ricci curvature and stretching those with negative Ricci curvature, effectively increasing the separation between clusters. Inspired by these two cornerstone works, we propose a geometry-based RAG reranker enhancement procedure called Ricci-Filtration. By modeling the input query and initial retrieved chunks as a network, where the input query and chunks serve as nodes and embedding-based pairwise relations define an initial graph, Ricci-Filtration leverages discrete curvature and Ricci flow to evaluate the structural importance of each chunk with respect to the user query. The system first filters the initial chunks based on their geometric curvature relative to the query; then, a reranker processes the remaining chunks to enhance generative performance. We theoretically prove that normalized discrete Ricci flow can detect community structures by identifying distinct asymptotic behaviors in edge weights. This supports the removal of ``noisy'' document chunks characterized by large weights and negative Ricci curvature relative to the query node. Extensive experiments confirm that Ricci-Filtration outperforms several baseline reranking methods in accuracy, precision, recall, and F1 scores. Furthermore, ablation studies demonstrate that the Ricci-Filtration generally outperforms the baseline under various settings, highlighting the framework's robustness across different architectures.

---

## 论文详细总结（自动生成）

好的，资深学术论文分析助手为您服务。以下是对论文《Ricci-Filtration: Boosting Retrieval-Augmented Generation Reranker to Query-Answer Tasks by Discrete Ricci Flow》的结构化、深入、客观总结。

### 1. 论文的核心问题与整体含义

本研究旨在解决**检索增强生成（RAG）系统中，由于初始检索不精确，导致输入给大语言模型（LLM）的上下文包含噪声或不相关文档块，进而损害生成答案质量**这一核心问题。

*   **研究背景与动机**：
    *   RAG 是解决 LLM 知识局限性和幻觉问题的常用方法，其流程分为“检索-增强-生成”三步。
    *   在“增强”阶段，通常采用**重排序器**对初步检索到的文档块进行细粒度筛选。现有重排序方法（如基于交叉编码器或LLM本身的重排序）存在灵活性不足（需要预设固定选取数量`k`）、计算成本高或延迟大的问题。
    *   特别是，固定的`k`值无法适应不同查询与文档块之间复杂多变的关系：`k`太小可能遗漏关键信息，`k`太大则会引入噪声，误导LLM。
*   **论文的整体含义**：提出一种**基于微分几何学原理**的新型RAG增强框架——**Ricci-Filtration**，利用离散Ricci流（Discrete Ricci Flow）来动态识别并过滤与查询在几何结构上不相关的文档块，旨在以更本质的方式提升重排序的精度和灵活性。

### 2. 论文提出的方法论

Ricci-Filtration 的核心思想是将**信息检索问题转化为图上的社区检测问题**，利用离散Ricci流来区分与查询“同社区”（相关）和“异社区”（噪声）的文档块。

*   **核心思想与流程**：
    1.  **图构建**：将用户查询和初步检索到的文档块作为图的节点。利用它们的嵌入向量计算**余弦不相似度**，并设定一个阈值，将高于此阈值的节点对连接起来，形成一个加权图。查询节点与所有文档块节点都被强制连接，以确保信息完整。
    2.  **离散Ricci流迭代**：在构建的图上运行有限次数的归一化离散Ricci流。该流程会迭代更新每条边的权重：具有**正Ricci曲率**的边（通常指向社区内部连接）会**收缩**，而具有**负Ricci曲率**的边（通常指向社区间连接）会**拉伸**。
    3.  **动态过滤（手术）**：迭代结束后，观察连接查询节点与各文档块节点的边的最终权重。根据归一化阈值（实验设为`η=1`），**移除那些最终边权重大于阈值（即具有负Ricci曲率）的边所对应的文档块**，认为它们是与查询“几何分离”的噪声块。如此，剩余文档块的数量是动态确定的。
    4.  **重排序与生成**：将过滤后的文档块输入一个传统的重排序器（如交叉编码器），进行最终排序，再交由LLM生成答案。

*   **关键技术细节与公式**：
    *   **离散Ollivier-Ricci曲率**：边`xy`的曲率`κxy`的计算公式为 `κxy = 1 − W(mx, my) / d(x, y)`。`d(x,y)`是节点间的图距离，`W(mx, my)`是节点`x`和`y`上概率分布的**Wasserstein距离**，它衡量将`x`处的质量最优迁移到`y`所需的最小总成本。
    *   **节点概率分布**：节点`x`上的概率质量分布`mx`为其自身和邻居分配权重，参数`α`控制留在自身的质量，`p`控制距离对邻居权重的影响。实验采用`α=0.5, p=2`。
    *   **离散Ricci流更新公式**：边权重`wxy`在第`i+1`次迭代的更新公式为 `w(i+1)xy = (1 - ε · κ(i)xy) · w(i)xy`，其中`ε`是步长。然后在每次迭代后进行归一化，使所有边的平均权重为1。
    *   **理论支撑**：论文给出了两个定理/命题，证明在某些特定图族上，归一化离散Ricci流能够以不同速率收缩/拉伸社区内外的边，从而在理论上保证了该方法的分离能力，为“手术”式过滤提供了依据。

### 3. 实验设计

*   **数据集与场景**：
    *   **单跳问答**：**SQuAD2.0** 和 **TriviaQA**。
    *   **多跳问答**：**HotpotQA**、**MultiHop-RAG** 和 **MuSiQue**。
*   **评估基准**：
    *   RAG中，检索采用基于余弦相似度的稠密检索，初始检索选前20个候选块。
    *   生成模型使用 `Llama-3.1-8B-Instruct` 和 `gpt-4o-mini` 两种LLM以测试鲁棒性。
    *   评估指标包括**准确率**、**精确率**、**召回率**和**F1分数**。
*   **对比方法**：
    *   **无重排序器**：直接将检索的前5个块输入LLM。
    *   **交叉编码器重排序器**：使用 `bge-reranker-base` 对前20个块重排序，选前5个。
    *   **LLM重排序器**：使用 `gpt-4o-mini` 作为重排序打分模型，选前5个。
    *   **Ricci-Filtration**：先经Ricci流动态过滤噪声块，再将剩余块用与交叉编码器基线相同的 `bge-reranker-base` 重排序。

### 4. 资源与算力

*   **文中未明确提及**执行实验所使用的具体算力资源，如GPU型号、数量或训练时长。文中指出核心成本在于Ricci流迭代和最优传输问题（Wasserstein距离）的计算，并在附录中与基线方法对比了单条查询的平均运行时长，但没有详细说明硬件环境。

### 5. 实验数量与充分性

*   **实验数量**：
    *   论文在**5个不同数据集**上进行了主实验。
    *   针对**2种不同的生成LLM**进行了鲁棒性测试。
    *   进行了**1组核心消融研究**（在TriviaQA上），详细探索了图构建阈值、Ricci流迭代次数、节点分布参数`p`、嵌入模型和重排序器模型等**7个关键变量**对性能的影响。
    *   在MuSiQue数据集上进行了**补充消融研究**。
    *   还设置了**K-means聚类过滤**作为几何过滤方法的对比基线。
*   **充分性、客观性与公平性评估**：
    *   **充分性强**：实验覆盖了单跳和多跳问答等多种任务，消融研究考察维度全面，能够有效验证方法的有效性、鲁棒性和各组件的贡献。
    *   **客观且公平**：所有对比方法均在相同的检索结果、生成模型和评估指标下进行，保证了对比的公平性。研究坦诚展示了如时间效率低和在部分多跳任务上效果不佳等局限性。

### 6. 论文的主要结论与发现

1.  **整体性能优越**：在大多数数据集和指标上，Ricci-Filtration均优于无重排序器和交叉编码器基线，尤其在SQuAD2.0和MultiHop-RAG的“Null”类查询上表现显著。
2.  **动态过滤有效**：该方法能够根据查询与文档块的几何结构关系，动态决定保留多少文档块，比传统的固定数量选择`k`更灵活、更合理。
3.  **鲁棒性强**：在不同生成LLM、不同嵌入模型和不同重排序器模型下，Ricci-Filtration均展现出性能优势，验证了框架的鲁棒性。
4.  **几何方法的潜力**：证明了将离散Ricci流应用于RAG去噪的可行性，为几何方法与信息检索的结合开辟了新方向。

### 7. 优点

*   **创新性**：首次将微分几何中的**离散Ricci流**应用于RAG流程，为解决信息过载和噪声问题提供了全新视角。
*   **自适应动态过滤**：实现了**动态确定保留文档块数量**的机制，避免了传统方法中固定`k`值的僵化。
*   **可解释性强**：通过Ricci曲率这一明确的几何概念来量化文档块与查询的相关性，其物理意义清晰，而非简单的“黑箱”分数。
*   **算法集成简便**：该方法作为一个预处理步骤，可以**轻松集成到现有的RAG管道**中，与各种嵌入模型和重排序器兼容。
*   **理论与实验结合**：既有理论证明支持方法的合理性，又有充分的实验验证其实际效果，论证扎实。

### 8. 不足与局限

*   **高计算延迟**：这是最显著的局限。由于需要在每次迭代中计算Wasserstein距离，Ricci流的计算代价高昂，导致单条查询的处理时间远高于交叉编码器基线（如其附录H所示，约是交叉编码器的8倍），**在实时性要求高的场景下应用受限**。
*   **多跳推理任务上的局限性**：在HotpotQA和MuSiQue等复杂多跳推理任务上，性能提升不明显甚至有所下降。当前方法倾向于保留过多文档块，可能引入了干扰多步骤逻辑连接的噪声。
*   **对嵌入质量的高度敏感**：初始图结构完全基于嵌入的余弦相似度构建。如果底层嵌入模型质量不高，生成的图无法正确反映语义关系，后续的Ricci流过滤也将失效。
*   **启发式阈值**：图构建阈值和过滤“手术”阈值的选择目前是经验性的，**缺乏普适的理论指导或自动学习机制**，这可能会影响方法在不同数据分布上的稳定性。
*   **理论保证的局限性**：理论证明仅限于特定对称图族和简化的参数设置（α=0, p=0），不能完全保证该方法在更一般、复杂的任意语义图上一定收敛或最优。

（完）
